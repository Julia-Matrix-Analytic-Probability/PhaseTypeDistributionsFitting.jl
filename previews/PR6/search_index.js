var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"Index of every documented name. Docstrings are rendered inline on the Fitting PH distributions page.\n\nModules = [PhaseTypeDistributionsFitting]","title":"API reference"},{"category":"section","location":"maph/#Fitting-MAPH-distributions","page":"Fitting MAPH distributions","text":"A multi-absorbing phase-type (MAPH) distribution is the joint law of the pair (τ, κ) — the absorption time and the index of the absorbing state reached — of a continuous-time Markov chain with m transient phases and n ≥ 1 absorbing states. It is the natural phase-type model for competing-risks data: τ is the time of the event and κ its cause. The distribution type is MAPHDist from PhaseTypeDistributions.jl, parameterized by (α, T, D) with α the initial distribution, T the m × m sub-generator and D the m × n matrix of absorption rates.\n\nGiven i.i.d. observations of (τ, κ) alone — the trajectory of the underlying chain is latent — fit_mle(MAPHDist, data; ...) estimates (α, T, D) with the approximate EM algorithm of the accompanying paper.","title":"Fitting MAPH distributions"},{"category":"section","location":"maph/#Quick-start","page":"Fitting MAPH distributions","text":"using PhaseTypeDistributions, PhaseTypeDistributionsFitting, Distributions\n\n# Synthetic competing-risks data from a known MAPH (m = 3 phases, n = 2 causes).\nT = [-3.0  1.0  0.5;\n      0.8 -2.5  0.7;\n      0.4  0.6 -2.0]\nD = [1.0 0.5;\n     0.4 0.6;\n     0.3 0.7]\ntruth = MAPHDist([0.5, 0.3, 0.2], T, D)\ndata = rand(truth, 2000)        # Vector of (t, k) pairs\n\n# Fit with 3 phases. The number of causes is read off the data.\nfitted = fit_mle(MAPHDist, data; m = 3)\n\n# MAPH distributions are not identifiable — compare through summaries:\nmarginal_absorption(fitted)      # ≈ empirical cause frequencies\nconditional_time(fitted, 1)      # conditional τ | κ = 1, as a PHDist\npdf(fitted, 1.3, 2)              # joint sub-density f(t, k)\n\nThe data format is a vector of (t, k) tuples with t > 0 and k a positive cause index — exactly what rand(::MAPHDist, L) returns.","title":"Quick start"},{"category":"section","location":"maph/#Right-censored-observations","page":"Fitting MAPH distributions","text":"Real competing-risks data are rarely complete: some subjects leave the study before their event, and all that is known about them is that they were still event-free at some time c. Pass those censoring times as censored:\n\n# 25% of subjects are censored at a fixed administrative horizon.\nsim = rand_censored(truth, 2000, 1.5)         # (events, censored)\n\nfitted = fit_mle(MAPHDist, sim.events; m = 3, censored = sim.censored)\n\nA censored record at c is the observation {τ > c}: it contributes the survival α exp(Tc) 1 to the likelihood, and in the E-step its latent path is completed through its eventual absorption — both before and after c — with its sufficient statistics spread over every possible eventual cause in proportion to the current model's posterior probability of reaching it. The complete-data likelihood and the M-step are therefore exactly the ones used for exact events; only the E-step changes.\n\nTwo assumptions matter. The censoring must be independent of (τ, κ) and non-informative, so that its law contributes no MAPH parameters; and at least one exact event must be present, since the number of causes and the reference cause of the second parameterization are read off the events.\n\nwarning: Warning\nDo not encode a censoring time as an event time. Doing so both shortens the fitted time scale and attributes the record to a cause it may never have reached — the fitted absorption probabilities and conditional means are then badly biased.","title":"Right-censored observations"},{"category":"section","location":"maph/#The-algorithm","page":"Fitting MAPH distributions","text":"Each iteration performs the steps of the paper's approximate EM:\n\nE-step. For each exact observation (t, k), the conditional expectations of the complete-data sufficient statistics — the start-phase indicator B̄, holding times Z̄, transient jump counts M̄, and absorbing-jump indicator Ē — are computed exactly from one 2m × 2m Van Loan block-matrix exponential, the same device as the PH E-step (see The EM algorithm) with the exit vector replaced by the observed cause's column D[:, k]. Each right-censored record at c costs two such exponentials — not one per cause: because the columns of R = -T⁻¹D sum to 1, the all-cause totals collapse onto a single evaluation and only the reference-cause slice needs a second.\nRelaxed M-step. In the second parameterization (α, q, R, U) — exit rates q, absorption-probability matrix R = -T⁻¹D, and the conditional one-step jump matrix U taken with respect to a reference cause (the most frequent one in the data) — the relaxed complete-data likelihood maximizer is available in closed form.\nConstraint enforcement. The relaxed U may leave the polytope on which (α, q, R, U) corresponds to a valid MAPH (equivalently, on which D = -T·R ≥ 0). Violating rows are returned to it by an ℓ1-projection — a small row-separable linear program solved with HiGHS. How often this fires is reported by the internal EM result (nprojections) and is a useful diagnostic for how binding the constraints are.\nConversion. (α, q, R, U) is mapped back to (α, T, D) for the next E-step.\n\nBecause the M-step maximizes a relaxed surrogate and then projects, the observed-data log-likelihood is not guaranteed to increase monotonically; convergence is declared when its change stays below tol for a few consecutive iterations.","title":"The algorithm"},{"category":"section","location":"maph/#Initialization","page":"Fitting MAPH distributions","text":"Two data-driven starting points are provided (Appendix C of the paper):\n\nmaph_moment_init (init_method = :moment): routes an exponential front end into per-cause hyper-/hypo-exponential blocks so that the empirical absorption probabilities and the per-cause conditional means and SCVs are matched (exactly, up to a small regularization that restores reachability and strict positivity of α).\nmaph_simplified_init (init_method = :simplified): matches the absorption probabilities and the overall mean only, with every parameter strictly positive. A deterministic per-phase rate jitter breaks the phase exchangeability of the textbook construction — without it the EM could never differentiate the phases.\n\nThe default init_method = :auto picks :moment for complete data and :simplified when there is censoring. The reason is that the moment initialization targets the conditional mean and SCV of τ | κ = k, which the raw event-time moments no longer estimate once records are censored — they see only the short times. The simplified initialization instead uses the observed-event proportions and the exposure-per-event scale (Σ yℓ)/#events, both of which stay valid under independent censoring. To use the moment initialization on censored data anyway, supply externally adjusted targets:\n\nfit_mle(MAPHDist, events; m = 4, censored = cens, init_method = :moment,\n        init_kwargs = (cond_means = μ̂, cond_scvs = ĉ²))\n\nYou can also pass any MAPHDist as init. Structural zeros of α and of the off-diagonal of T are EM-invariant and survive the fit; zeros of D are not preserved (it is regenerated from (q, R, U) each iteration).","title":"Initialization"},{"category":"section","location":"maph/#API","page":"Fitting MAPH distributions","text":"The fit router also accepts MAPHDist with method = :mle.","title":"API"},{"category":"method","location":"maph/#Distributions.fit_mle-Tuple{Type{MAPHDist}, AbstractVector{<:Tuple{var\"#s6\", var\"#s5\"} where {var\"#s6\"<:Real, var\"#s5\"<:Integer}}}","page":"Fitting MAPH distributions","text":"fit_mle(MAPHDist, data; m, init, censored=Float64[], init_method=:auto,\n        init_kwargs=(), maxiter=500, tol=1e-7, verbose=false)\n\nFit a multi-absorbing phase-type distribution to competing-risks observations data — a vector of pairs (t, k) with t > 0 the absorption time and k ∈ 1..n the index of the absorbing cause (as returned by rand(::MAPHDist, L)) — by the approximate EM algorithm of the paper: an exact E-step via Van Loan matrix exponentials, a closed-form relaxed M-step in the (α, q, R, U) parameterization, and an ℓ1-projection linear program (HiGHS) that re-enforces the feasibility constraints whenever the relaxed update leaves the valid parameter region.\n\nProvide either the number of transient phases m — the starting point is then built by maph_moment_init (init_method = :moment, matching per-cause means and SCVs) or maph_simplified_init (init_method = :simplified) — or an explicit init::MAPHDist. The default init_method = :auto picks :moment for uncensored data and :simplified when there is censoring (see below). Extra keyword arguments for the chosen initializer go in the named tuple init_kwargs. The number of absorbing states is read off the data (the largest cause index observed) unless init provides more. The reference cause of the second parameterization is the most frequent observed event cause; it must be reachable from every phase of the starting distribution (both built-in initializers guarantee this).\n\nRight-censored observations\n\nPass the right-censoring times as censored. A censored record at time c contributes the observation {τ > c} — the subject is known to have survived past c, and its eventual cause is unobserved — so it adds log(α exp(Tc) 1) to the log-likelihood and enters the E-step through the cause-resolved censored expectations of the paper, which complete the latent path on both sides of c and spread it fractionally over every possible eventual cause. The censoring is assumed independent of (τ, κ) and non-informative, so its law contributes no MAPH parameters.\n\nfit_mle(MAPHDist, events; m = 3, censored = [3.0, 5.5, 2.1])\n\nAt least one exact event is required, since the cause count and the reference cause are read off the events. Because the per-cause event-time moments are biased under censoring, init_method = :moment then needs censoring-adjusted targets, supplied as init_kwargs = (cond_means = ..., cond_scvs = ...); the :auto default sidesteps this by using the censoring-compatible maph_simplified_init.\n\nReturns a MAPHDist. Because the M-step optimizes a relaxed surrogate followed by a projection, the log-likelihood is not guaranteed to increase at every iteration; convergence is declared when its change drops below tol. MAPH distributions are not identifiable, so compare fits through distributional summaries (absorption probabilities, conditional moments, cdfs) rather than through (α, T, D) directly.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"function","location":"maph/#PhaseTypeDistributionsFitting.maph_moment_init","page":"Fitting MAPH distributions","text":"maph_moment_init(m, data; censored=Float64[], cond_means=nothing,\n                 cond_scvs=nothing, omega_factor=10.0, epsilon=0.01,\n                 theta=nothing, beta=0.5) -> MAPHDist\n\nMoment-based initialization (Appendix C of the paper). The transient phases split into a front end S₁ (each phase exits after an Exp(ω) sojourn) and per-cause phase-type blocks: leaving S₁, the chain routes into the block of cause k — a two-phase hyper-exponential (c²ₖ > 1), a hypo-exponential (c²ₖ < 1) or a single exponential (c²ₖ ≈ 1), built by the corresponding PhaseTypeDistributions constructors — or, for causes the phase budget cannot accommodate, directly into the absorbing state. At epsilon = theta = 0 the construction matches the empirical absorption probabilities exactly, and the conditional mean and SCV of every block-fitted cause exactly.\n\nω is set to omega_factor (> 1) times the threshold of Appendix C, which guarantees the corrected block targets are valid. Larger factors shrink the front end's share 1/ω of each conditional mean, keeping the construction closer to the exact per-cause moment match (empirically a better EM starting point), at the price of a stiffer generator in the E-step matrix exponentials. The (epsilon, theta) regularization mixes epsilon of a uniform distribution into α and adds a total feedback rate theta from every block phase to the front end (default 0.01/μ̄), making every phase carry positive initial mass and every absorbing state reachable from every phase — as the EM machinery requires — at the cost of an O(epsilon + theta) perturbation of the matched moments.\n\nCauses are prioritized for blocks in decreasing order of empirical frequency. If not even the most frequent cause fits the budget (p = 0, e.g. m ≤ 2), this falls back to maph_simplified_init with routing constant beta.\n\nRight censoring. The per-cause targets are the conditional mean and SCV of τ | κ = k, and the empirical event-time moments estimate them only when nothing is censored — under censoring they see just the short times and are biased downwards. Passing a non-empty censored therefore requires supplying censoring-adjusted or external targets through cond_means and cond_scvs (length-n vectors, NaN for causes to leave unfitted); otherwise use maph_simplified_init, which is censoring-compatible. The exposure-per-event scale from censored is still used for the default theta.\n\n\n\n\n\n","title":"PhaseTypeDistributionsFitting.maph_moment_init"},{"category":"function","location":"maph/#PhaseTypeDistributionsFitting.maph_simplified_init","page":"Fitting MAPH distributions","text":"maph_simplified_init(m, data; censored=Float64[], beta=0.5, jitter=0.5) -> MAPHDist\n\nSimplified initialization (Appendix C of the paper): every phase has sojourn rate ω; after a sojourn the chain moves to another transient phase with probability beta (uniformly) and absorbs with probability 1 - beta, into cause k with probability π̂ₖ. Since a geometric sum of i.i.d. exponentials is exponential, τ ~ Exp((1-beta)·ω) independently of κ, and choosing ω = 1/((1-beta)·μ̄) matches the scale μ̄ exactly, while the absorption probabilities match π̂ exactly. All parameters are strictly positive (for causes present in the data), so the reachability assumption holds and no parameter starts on the boundary. For m = 1, beta is forced to 0 (there is no other phase to route to).\n\nThis is the censoring-compatible initialization: pass the right-censored times as censored and π̂, μ̄ become the observed-event proportions and the exposure-per-event scale of _maph_cause_statistics, which reduce to the sample cause frequencies and the sample mean when nothing is censored.\n\nAt jitter = 0 the construction is exactly as above — but then all phases are exchangeable, and since the EM update preserves exchangeability, an EM run started there can never differentiate the phases (it is confined to fits with τ exponential and independent of κ). The default jitter > 0 therefore applies a deterministic symmetry-breaking perturbation: the sojourn rate of phase i is scaled by 1 + jitter·zᵢ with zᵢ spread evenly over [-1, 1]. The perturbation leaves the embedded jump chain — and hence the absorption probabilities π̂ — untouched, and the generator is rescaled afterwards so the overall mean stays exactly μ̄; only the exponential shape of τ (and its independence of κ) becomes approximate.\n\n\n\n\n\n","title":"PhaseTypeDistributionsFitting.maph_simplified_init"},{"category":"section","location":"fitting/#Fitting-PH-distributions","page":"Fitting PH distributions","text":"All estimators follow the Distributions.jl convention: dispatch on the target type and return a new distribution of that type. The target is either the general PHDist or one of the structured families (CoxianDist, HyperExponentialDist, HypoExponentialDist, ErlangPHDist), which constrains the fit to that family.\n\nusing PhaseTypeDistributions, PhaseTypeDistributionsFitting, Distributions, Random, Statistics\nrng = MersenneTwister(2024)\n\ntruth = HyperExponentialDist([0.6, 0.4], [1.0, 0.25])   # mean 1.0, SCV > 1\ndata  = rand(rng, truth, 5000)\n(empirical_mean = Statistics.mean(data), empirical_scv = var(data) / Statistics.mean(data)^2)","title":"Fitting PH distributions"},{"category":"section","location":"fitting/#General-PH-fit","page":"Fitting PH distributions","text":"Give the number of phases m for a random dense start, or an init distribution to start from (and to impose structure — see below).\n\nphd = fit_mle(PHDist, data; m = 3)\n(mean = mean(phd), scv = scv(phd))","title":"General PH fit"},{"category":"section","location":"fitting/#Structured-fits-stay-in-family","page":"Fitting PH distributions","text":"Each structured target returns its own type and keeps that family's structure throughout the fit:\n\ncox = fit_mle(CoxianDist, data; m = 3)\nhyp = fit_mle(HyperExponentialDist, data; m = 2)\nhpo = fit_mle(HypoExponentialDist, data; m = 3)\nerl = fit_mle(ErlangPHDist, data; m = 3)     # closed form: rate = m / mean(data)\ntypeof.((cox, hyp, hpo, erl))\n\nmean.((cox, hyp, hpo, erl))\n\nBecause PH distributions are not identifiable, compare fits by moments or CDF rather than by (α, T):\n\nmoments_isapprox(phd, hyp; rtol = 0.1), distribution_isapprox(phd, hyp; atol = 0.05)","title":"Structured fits stay in-family"},{"category":"section","location":"fitting/#Supplying-an-initial-distribution","page":"Fitting PH distributions","text":"Any AbstractPHDist can be the starting point. Its sparsity pattern is preserved for the whole run (see The EM algorithm), so seeding a general PHDist fit with a structured distribution imposes that structure:\n\nphd_cox = fit_mle(PHDist, data; init = CoxianDist([3.0, 2.0, 1.0], [0.3, 0.4]))\npattern(subgenerator(phd_cox))     # bidiagonal — the Coxian structure is locked\n\nThe loop is controlled with maxiter, tol, verbose, and rng:\n\nres = fit_mle(PHDist, data; m = 2, maxiter = 500, tol = 1e-9)\nmean(res)","title":"Supplying an initial distribution"},{"category":"section","location":"fitting/#The-fit-router","page":"Fitting PH distributions","text":"fit routes to fit_mle (method = :mle, the default) or fit_mm (method = :mm, method of moments):\n\nf = fit(PHDist, data; method = :mle, m = 2)\nmean(f)","title":"The fit router"},{"category":"section","location":"fitting/#API","page":"Fitting PH distributions","text":"","title":"API"},{"category":"method","location":"fitting/#Distributions.fit_mle-Tuple{Type{PHDist}, AbstractVector{<:Real}}","page":"Fitting PH distributions","text":"fit_mle(PHDist, data; m, init, maxiter=1000, tol=1e-7, verbose=false, rng)\n\nFit a general phase-type distribution to positive observations data by maximum likelihood using the EM algorithm (Asmussen–Nerman–Olsson). Provide the number of phases m for a dense random start, or an init distribution (any AbstractPHDist) — in which case the sparsity pattern of init is preserved, so explicit zeros stay zero throughout the fit.\n\nReturns a PHDist whose α and T are FixedSparsity arrays carrying the preserved sparsity pattern. Phase-type distributions are not identifiable, so compare fits by moments or CDF (moments_isapprox, distribution_isapprox) rather than by (α, T) directly.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"fitting/#Distributions.fit_mle-Tuple{Type{CoxianDist}, AbstractVector{<:Real}}","page":"Fitting PH distributions","text":"fit_mle(CoxianDist, data; m, init, maxiter=1000, tol=1e-7, verbose=false, rng)\n\nFit a Coxian PH distribution (α = e₁, bidiagonal T, exit possible at each phase). The Coxian structure is preserved by the EM and a CoxianDist is returned.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"fitting/#Distributions.fit_mle-Tuple{Type{HyperExponentialDist}, AbstractVector{<:Real}}","page":"Fitting PH distributions","text":"fit_mle(HyperExponentialDist, data; m, init, maxiter=1000, tol=1e-7, verbose=false, rng)\n\nFit a hyperexponential (mixture of m exponentials; diagonal T). Returns a HyperExponentialDist. Note SCV ≥ 1 is intrinsic to this family.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"fitting/#Distributions.fit_mle-Tuple{Type{HypoExponentialDist}, AbstractVector{<:Real}}","page":"Fitting PH distributions","text":"fit_mle(HypoExponentialDist, data; m, init, maxiter=1000, tol=1e-7, verbose=false, rng)\n\nFit a hypoexponential (convolution of m exponentials; α = e₁, absorption only from the last phase). Returns a HypoExponentialDist. Note SCV ≤ 1 is intrinsic.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"fitting/#Distributions.fit_mle-Tuple{Type{ErlangPHDist}, AbstractVector{<:Real}}","page":"Fitting PH distributions","text":"fit_mle(ErlangPHDist, data; m)\n\nFit an Erlang (m phases of equal rate) by maximum likelihood. The shape m is fixed; the rate has the closed form λ̂ = m / mean(data), so no EM is required. Returns an ErlangPHDist.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"fitting/#StatsAPI.fit-Union{Tuple{D}, Tuple{Type{D}, AbstractVector{<:Real}}} where D<:AbstractPHDist","page":"Fitting PH distributions","text":"fit(T, data; method=:mle, kwargs...)\n\nConvenience entry point routing to fit_mle (method=:mle, the default, maximum likelihood via EM) or fit_mm (method=:mm, method of moments). Extra keyword arguments are forwarded to the chosen estimator.\n\n\n\n\n\n","title":"StatsAPI.fit"},{"category":"function","location":"fitting/#PhaseTypeDistributionsFitting.fit_mm","page":"Fitting PH distributions","text":"fit_mm(T, data; …)\n\nMoment-based fitting of a phase-type distribution. Not yet implemented — this is a placeholder for the moment-matching approach, which will land alongside the EM-based fit_mle. Use fit_mle in the meantime.\n\n\n\n\n\n","title":"PhaseTypeDistributionsFitting.fit_mm"},{"category":"section","location":"em/#The-EM-algorithm","page":"The EM algorithm","text":"The maximum-likelihood fit uses the classic Asmussen–Nerman–Olsson EM algorithm for phase-type distributions.","title":"The EM algorithm"},{"category":"section","location":"em/#The-complete-data-model","page":"The EM algorithm","text":"The complete data for one observation x is the trajectory of the underlying Markov jump process up to absorption: which phase it starts in, how long it spends in each phase, and which transitions it makes. The complete-data log-likelihood is linear in four sufficient statistics:\n\nB_i — number of starts in phase i (informs α),\nZ_i — total time spent in phase i (the denominators of the rates),\nM_ij — number of i to j transitions, i neq j (informs T),\nN_i — number of i to absorption transitions (informs t^0).","title":"The complete-data model"},{"category":"section","location":"em/#Exact-E-step-via-a-Van-Loan-block-exponential","page":"The EM algorithm","text":"For f(x) = α^ exp(Tx) t^0 define the row and column vectors a(x) = α^exp(Tx) and b(x) = exp(Tx)t^0, and\n\nC(x) = int_0^x exp(T^ u)(α t^0)exp(T^(x-u))mathrmdu \n\nBy Van Loan's identity, C(x) is exactly the top-right m times m block of\n\nexpleft( beginbmatrix T^  αt^0  0  T^ endbmatrix x right)\n\nso a single 2m times 2m matrix exponential per observation yields every expected sufficient statistic, conditional on the observed absorption time:\n\nbar B_i = fracα_i b_i(x)f(x) quad\nbar Z_i = fracC_iif(x) quad\nbar M_ij = fracT_ij C_ijf(x) quad\nbar N_i = fraca_i(x) t^0_if(x) \n\nThis is exact — no quadrature or ODE integration — and the observed-data log-likelihood sum_ell log f(x_ell) falls out at the same time.","title":"Exact E-step via a Van Loan block exponential"},{"category":"section","location":"em/#M-step-and-convergence","page":"The EM algorithm","text":"Summing the per-observation statistics over the data set, the M-step is closed-form:\n\nα_i = fracbar B_iL qquad\nt^0_i = fracbar N_ibar Z_i qquad\nT_ij = fracbar M_ijbar Z_i (i neq j) qquad\nT_ii = -Big(t^0_i + sum_j neq i T_ijBig) \n\nThe loop alternates E- and M-steps until the increase in log-likelihood drops below tol (or maxiter is reached). EM increases the observed-data likelihood monotonically, a useful internal sanity check.","title":"M-step and convergence"},{"category":"section","location":"em/#Structure-preservation","page":"The EM algorithm","text":"Because bar B propto α, bar M propto T_textoff and bar N propto t^0, a 0 factor produces a 0 statistic. On top of that the M-step is pattern-aware: it is handed the sparsity pattern of the starting parameters — the allowed positions of α, of the off-diagonal of T, and of the exit vector t^0 — and writes only into those positions.\n\nThe fitted parameters are returned as the fixed-sparsity arrays from FixedSparsityMatrices.jl: α as a FixedSparsityVector and T as a FixedSparsityMatrix. The pattern is therefore not merely preserved by arithmetic but enforced by the type — writing a nonzero into a fixed-zero entry throws.\n\nusing PhaseTypeDistributions, PhaseTypeDistributionsFitting, Distributions, Random\nrng = MersenneTwister(7)\ndata = rand(rng, HyperExponentialDist([0.5, 0.5], [1.0, 0.3]), 2000)\n\n# Seed a general PHDist fit with a Coxian; the structure is locked in.\nphd = fit_mle(PHDist, data; init = CoxianDist([3.0, 2.0, 1.0], [0.3, 0.4]))\npattern(initial_prob(phd)), pattern(subgenerator(phd))\n\n# The fixed zeros are enforced: this is an error.\ntry\n    subgenerator(phd)[3, 1] = 0.5\ncatch err\n    err\nend\n\nA dense start has a full pattern, leaving the EM free to fill the whole matrix:\n\nphd_dense = fit_mle(PHDist, data; m = 3)\nall(pattern(subgenerator(phd_dense)))","title":"Structure preservation"},{"category":"section","location":"#PhaseTypeDistributionsFitting.jl","page":"Home","text":"Fitting algorithms for phase-type (PH) and multi-absorbing phase-type (MAPH) distributions. Companion package to PhaseTypeDistributions.jl, which provides the distribution types this package fits.\n\nThe package accompanies the paper Inference for Multi-Absorbing Phase Type Distributions, Algorithms, and Applications (Qiao, Surya, Asanjarani, Nazarathy; in preparation).","title":"PhaseTypeDistributionsFitting.jl"},{"category":"section","location":"#Background","page":"Home","text":"A continuous-time PH distribution is the distribution of the absorption time of a finite-state continuous-time Markov chain with one absorbing state. Given an initial distribution α over m transient phases and a sub-generator matrix T, the absorption time τ has density and tail\n\nf_τ(x) = α^ exp(Tx) t^0 qquad\nbar F_τ(x) = α^ exp(Tx) mathbf 1 qquad\nt^0 = -T mathbf 1 \n\nGiven i.i.d. samples x₁, …, x_L of τ, this package estimates (α, T) by maximum likelihood using the EM algorithm, with the structured PH families (CoxianDist, HyperExponentialDist, HypoExponentialDist, ErlangPHDist) available as constrained targets.","title":"Background"},{"category":"section","location":"#Scope","page":"Home","text":"Estimator Status Notes\nfit_mle (EM) — PH implemented exact E-step via a Van Loan block-matrix exponential\nfit_mle (EM) — MAPH implemented approximate EM: relaxed M-step + ℓ1-projection LP; see Fitting MAPH distributions\nfit_mm (moment matching) planned (stub) will also seed the EM","title":"Scope"},{"category":"section","location":"#Installation","page":"Home","text":"Not yet registered. During development, Pkg.develop it alongside its sibling packages:\n\nusing Pkg\nPkg.develop(path = \"path/to/FixedSparsityMatrices.jl\")\nPkg.develop(path = \"path/to/PhaseTypeDistributions.jl\")\nPkg.develop(path = \"path/to/PhaseTypeDistributionsFitting.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using PhaseTypeDistributions, PhaseTypeDistributionsFitting, Distributions, Random, Statistics\nrng = MersenneTwister(1)\n\n# Synthetic data from a known hyperexponential.\ndata = rand(rng, HyperExponentialDist([0.7, 0.3], [1.0, 0.2]), 5000)\n\nphd = fit_mle(PHDist, data; m = 3)          # general PH fit\nhyp = fit_mle(HyperExponentialDist, data; m = 2)\n\n(mean_data = Statistics.mean(data), mean_phd = mean(phd), mean_hyp = mean(hyp))\n\n# PH distributions are not identifiable — compare fits by moments / CDF.\nmoments_isapprox(phd, hyp; rtol = 0.1), distribution_isapprox(phd, hyp; atol = 0.05)","title":"Quick start"},{"category":"section","location":"#Navigation","page":"Home","text":"Fitting PH distributions — the full fitting API and worked examples.\nThe EM algorithm — how the exact E-step and M-step work, and structure preservation.\nAPI reference — index of every documented name.","title":"Navigation"},{"category":"section","location":"#Relationship-to-EMpht.jl","page":"Home","text":"An earlier Julia package for PH-EM is EMpht.jl, a port of Asmussen's EMpht.c. PhaseTypeDistributionsFitting.jl supersedes it in scope (PH and MAPH, integrated with Distributions.jl via PhaseTypeDistributions.jl) and in active maintenance. We draw on its ideas freely (structured-PH parameterizations, censoring) and credit them as we go.","title":"Relationship to EMpht.jl"}]
}
